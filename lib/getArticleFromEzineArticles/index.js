const log = require('lib/utils/logger')

const getArticleFromEzineArticles = async () => {
  // must search on google `site:ezinearticles.com ${keyword}`
  // do NOT use ezinearticles search
}

module.exports = getArticleFromEzineArticles
